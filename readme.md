# Pyload in the Docker

Free and Open Source download manager written in Python https://pyload.net

    docker-compose up -d
    docker-compose down

Webinterface http://localhost:8000/login

    USER=pyload
    PASSWORD=pyload